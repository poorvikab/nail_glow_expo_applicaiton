import React from 'react';
import { View, Text } from 'react-native';

/** Skia canvas with nail overlay — placeholder for future implementation */
export default function NailCanvas() {
  return <View />;
}
